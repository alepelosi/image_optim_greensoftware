[package]
name = "ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
energy-bench = { path = "../energy-bench", features = ["software-energy-lab"] }
indexmap = { version = "2.13.0", features = ["serde"] }

[build-dependencies]
cbindgen = "0.29.2"
